node_modules
.next
out
.env*.local
next-env.d.ts
.wrangler
tsconfig.tsbuildinfo

# Source video masters (4K). The compressed, web-sized derivatives in
# public/videos/ are what ship - these stay local so the repo does not carry
# tens of megabytes GitHub would warn about.
#
# EDITING A MASTER CHANGES NOTHING UNTIL IT IS RE-ENCODED.
# The current grade and settings are in README.md under "Hero video".

/*.mp4
